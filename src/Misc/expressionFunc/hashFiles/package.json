{
  "name": "hashFiles",
  "version": "1.0.0",
  "description": "GitHub Actions HashFiles() expression function",
  "main": "lib/hashFiles.js",
  "scripts": {
    "format": "prettier --write **/*.js",
    "format-check": "prettier --check **/*.js",
    "lint": "eslint src/**/*.js",
    "pack": "ncc build ./src/hashFiles.js -o ../../layoutbin/hashFiles",
    "all": "npm run format && npm run lint && npm run pack"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/runner.git"
  },
  "keywords": [
    "actions"
  ],
  "author": "GitHub Actions",
  "license": "MIT",
  "dependencies": {
    "@actions/glob": "^0.3.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0",
    "eslint": "^8.31.0",
    "eslint-plugin-github": "^4.6.0",
    "prettier": "^2.8.1"
  }
}
